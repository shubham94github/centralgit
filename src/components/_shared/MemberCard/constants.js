import { Icons } from '@icons';
import { colors } from '@colors';

export const menuIcon = Icons.threeDotsMenuIcon(colors.black);
export const userIcon = Icons.user();
export const bagIcon = Icons.bag();
export const locationIcon = Icons.locationIcon(colors.darkblue70);
export const emailIcon = Icons.emailIcon(colors.darkblue70);
